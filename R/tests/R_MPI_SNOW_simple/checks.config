# this only shows what is possible, the tests shown here are pretty trivial

# there is a global 'checks' directory in the root of your tests
stdout_exists=is_true.py exists(stdout.txt)
intermed40_exists=is_true.py exists(intermed40.txt)
finalresult_exists=is_true.py exists(finalresult.txt)


